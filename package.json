{
  "name": "macd-options-desk",
  "version": "1.0.0",
  "private": true,
  "description": "Upstox MACD options trading console — per-instrument controls and net-profit-after-charges calculation",
  "scripts": {
    "test": "node --test tests/charges.test.js",
    "serve": "python3 -m http.server 8080"
  }
}
